celery==5.6.3
coverage==7.15.4
# cryptography isn't imported directly anywhere in this codebase, but is
# kept as an explicit pin since it's a security-sensitive transitive
# dependency of PyJWT/django-allauth.
cryptography==50.0.0
django-admin-thumbnails==1.0.0
django-cors-headers==4.9.0
django-filter==26.1
django-webpack-loader==3.2.4
Django==6.0.8
djangorestframework-queryfields==1.1.0
djangorestframework-simplejwt==5.5.1
djangorestframework==3.18.0
drf-url-filters==0.5.1
editdistance==0.8.1
# ExifRead and piexif aren't imported directly anywhere in this codebase --
# they were removed as "zero references" in an earlier pass, which broke a
# genuinely fresh `pip install` (only caught once CI actually ran on a clean
# environment): gpsphoto's own setup.py doesn't declare install_requires at
# all, but gpsphoto.py itself does `import exifread` and
# `from piexif import load, dump` -- so they're required transitive
# dependencies of a package we do use, not actually unused.
ExifRead==3.5.1
piexif==1.1.3
flower==2.1.0
geopy==2.5.0
gpsphoto==2.2.3
gunicorn==26.1.0
imageio==2.37.4
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5
Pillow==12.3.0
pillow-heif==1.5.0
postgres==4.0
psycopg2-binary==2.9.12
PyJWT==2.13.0
python-crontab==3.3.0
python-dateutil==2.9.0.post0
pytz==2026.3.post1
redis==8.1.0
regex==2026.7.19
requests==2.34.2
scikit-learn==1.9.0
# Pinned to 1.18.0 deliberately, not the newer 1.18.1 -- see CLAUDE.md:
# upgrading alongside Django 6.1 caused the test suite to hang (root cause
# not confirmed to be scipy vs Django), so both are pinned to the last
# combination that passed all 93 tests.
scipy==1.18.0
torch==2.13.0
torchvision==0.28.0
xmltodict==1.0.4
onnxruntime==1.29.0
insightface==1.0.1
tqdm==4.70.0
# django-allauth[openidconnect]'s "openidconnect" extra is a no-op as of
# 65.19.1 (pip warns "does not provide the extra") -- the openid_connect
# provider now ships in the base package. Dropped the extra.
django-allauth==65.19.1
